# 环境配置
NODE_ENV=development
PORT=3000

# 数据库配置
# 本地 MongoDB
MONGO_ATLAS_URI=mongodb://localhost:27017/live-platform
# 或 MongoDB Atlas
# MONGO_ATLAS_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority

# JWT配置
JWT_SECRET=your-jwt-secret-key
JWT_EXPIRES_IN=7d

# 文件上传配置
UPLOAD_DIR=uploads
BASE_URL=http://localhost:3000

# 日志配置
LOG_LEVEL=debug

# 注意：
# 1. 确保在生产环境使用强密钥
# 2. MongoDB Atlas 连接字符串中的密码需要进行 URL 编码
# 3. 建议在生产环境使用环境变量或密钥管理服务存储敏感信息
